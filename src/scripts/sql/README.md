# dmptool-apollo-server src/scripts/sql

## SQL Helper Scripts

This directory contains SQL scripts that can be used to help set up and manage the database. They are intended to be run 
manually rather than automatically by the data migration processes. They can be used to help analyze problems, repair 
orphaned records, etc. 

